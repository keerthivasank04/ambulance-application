{
  "name": "ambulance-app-e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Selenium WebDriver end-to-end smoke suite for the TN 108 Ambulance app",
  "scripts": {
    "test": "mocha --reporter mochawesome --reporter-options reportDir=report,reportFilename=index,quiet=true \"tests/**/*.spec.js\"",
    "test:headed": "cross-env HEADLESS=false npm test"
  },
  "devDependencies": {
    "chai": "^5.1.2",
    "cross-env": "^7.0.3",
    "mocha": "^10.7.3",
    "mochawesome": "^7.1.3",
    "selenium-webdriver": "^4.27.0"
  }
}
